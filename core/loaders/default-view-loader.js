// Generated by CoffeeScript 1.4.0
(function() {

  define(['coala/core/view', 'coala/core/config'], function(View, config) {
    var getPath;
    getPath = config.getPath;
    return {
      type: 'view',
      name: 'DEFAULT',
      fn: function(module, feature, viewName, args) {
        var def, deferred, options;
        deferred = $.Deferred();
        options = args[0];
        if ((options != null ? options.avoidLoadingView : void 0) === true) {
          def = {
            baseName: viewName,
            module: module,
            feature: feature
          };
          if (options.avoidLoadingHandlers === true) {
            def.avoidLoadingHandlers = true;
          }
          deferred.resolve(new View(def));
          return deferred;
        }
        module.loadResource(getPath(feature, 'view', viewName)).done(function(def) {
          var view;
          if (def == null) {
            def = {};
          }
          def.baseName = viewName;
          def.module = module;
          def.feature = feature;
          view = new View(def);
          return deferred.resolve(view);
        });
        return deferred;
      }
    };
  });

}).call(this);
